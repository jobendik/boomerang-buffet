{
  "name": "boomerang-buffet",
  "private": true,
  "version": "1.0.0",
  "type": "module",
  "description": "An original arena brawler homage to the boomerang party-fighter genre.",
  "scripts": {
    "dev": "vite",
    "build": "tsc --noEmit && vite build",
    "preview": "vite preview",
    "test": "vitest run",
    "sim": "SIM_REPORT=1 vitest run tests/ai-report.test.ts",
    "typecheck": "tsc --noEmit"
  },
  "devDependencies": {
    "jsdom": "^29.1.1",
    "typescript": "^5.6.3",
    "vite": "^6.0.7",
    "vitest": "^4.1.10"
  },
  "dependencies": {
    "@fontsource-variable/nunito": "^5.2.7",
    "@fontsource/lilita-one": "^5.2.8"
  }
}
